{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/danilo-aguiar-br/sqlite-graphrag/schemas/prune-ner.schema.json",
  "title": "PruneNerResponse",
  "description": "Response emitted by `sqlite-graphrag prune-ner --json` on stdout as a single JSON line.",
  "type": "object",
  "required": ["action", "bindings_removed", "namespace", "elapsed_ms"],
  "additionalProperties": false,
  "properties": {
    "agent_surface": { "$ref": "https://github.com/danilo-aguiar-br/sqlite-graphrag/schemas/agent-surface.schema.json#/$defs/AgentSurfaceMeta" },
    "truncated": { "type": "boolean", "description": "GAP-SG-142: raised by the agent-native surface whenever it removed data from this envelope." },
    "count": { "type": "integer", "minimum": 0, "description": "GAP-SG-142: emitted under --count-only, which replaces the payload. See agent-surface.schema.json#/$defs/CountOnlyEnvelope." },
    "action":           { "type": "string", "enum": ["pruned", "dry_run", "aborted"], "description": "'pruned' when bindings were physically removed; 'dry_run' when --dry-run was passed; 'aborted' when --yes was not passed." },
    "bindings_removed": { "type": "integer", "minimum": 0, "description": "Number of memory-entity NER binding rows removed (or that would be removed in dry_run/aborted mode)." },
    "namespace":        { "type": "string", "description": "Namespace in which the prune was performed." },
    "entity":           { "type": "string", "description": "Entity name targeted when --entity was used." },
    "elapsed_ms":       { "type": "integer", "minimum": 0, "description": "Handler wall-clock time in milliseconds." }
  }
}
