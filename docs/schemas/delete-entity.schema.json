{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/danilo-aguiar-br/sqlite-graphrag/schemas/delete-entity.schema.json",
  "title": "DeleteEntityResponse",
  "description": "Response emitted by `sqlite-graphrag delete-entity --json` on stdout as a single JSON line.",
  "type": "object",
  "required": ["action", "entity_name", "namespace", "relationships_removed", "bindings_removed", "elapsed_ms"],
  "additionalProperties": false,
  "properties": {
    "agent_surface": { "$ref": "https://github.com/danilo-aguiar-br/sqlite-graphrag/schemas/agent-surface.schema.json#/$defs/AgentSurfaceMeta" },
    "truncated": { "type": "boolean", "description": "GAP-SG-142: raised by the agent-native surface whenever it removed data from this envelope." },
    "count": { "type": "integer", "minimum": 0, "description": "GAP-SG-142: emitted under --count-only, which replaces the payload. See agent-surface.schema.json#/$defs/CountOnlyEnvelope." },
    "action":                { "type": "string", "const": "deleted", "description": "Always 'deleted' on success." },
    "entity_name":           { "type": "string", "description": "Name of the entity that was permanently removed." },
    "namespace":             { "type": "string", "description": "Namespace in which the entity was deleted." },
    "relationships_removed": { "type": "integer", "minimum": 0, "description": "Number of graph relationships (edges) removed along with the entity." },
    "bindings_removed":      { "type": "integer", "minimum": 0, "description": "Number of memory-entity binding rows removed along with the entity." },
    "elapsed_ms":            { "type": "integer", "minimum": 0, "description": "Handler wall-clock time in milliseconds." }
  }
}
